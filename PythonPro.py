#!/usr/bin/python3
import Basic_Maths, dataclasses, signal, s2clientprotocol, time, os, ossaudiodev, _osx_support, setuptools, servicemanager, cachetools, cgi, sys, symtable, sysconfig, syslog, linecache, ldap3, lib2to3, locale, _threading_local, logging, lzma, shelve, farmhash, gdb, qrcode, openpyxl, opentracing, operator, opcode, _operator, optparse, base64, binascii, bisect, bs4, hashlib, hdbcli, jack, json, tkinter, _tkinter, tempfile, termios, deprecated, queue, quopri, csv, _csv, colorama, cProfile, abc, farmhash, flake8_2020, flake8_builtins, flake8_simplify

def p(string):
    print("{0}", format(string))

def i(inputstr):
    global inputs
    inputs=str(input('{0}', format(inputstr)))